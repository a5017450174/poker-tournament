# 德州撲克之底牌驅逐令 — 設計文件 & 進度紀錄

> 多人連線版的完整設計、實作現況、踩過的坑、以及單機版（roguelike 風格）的方向建議。
>
> 這份文件可以丟給 AI 或工程師當作專案 onboarding，也可以當作未來單機版的設計起點。

---

## 一、遊戲概念

**標題**：德州撲克之底牌驅逐令
**Slogan**：用你的底牌，把別人通通趕出牌桌

**核心 Pitch**：標準德州撲克 + 角色天賦 + 能力系統的 meta layer。每位玩家有一個整局生效的「角色天賦」，過程中靠 KO 對手 + 升盲階段獎勵獲得「能力」加強自己。錦標賽制，把對手全部打到 0 籌碼為止。

**目前狀態**：多人連線版可玩，部署在 Render 免費版（https://poker-tournament-4n94.onrender.com/）。已經支援桌面 + 手機 RWD。

---

## 二、技術架構

### 檔案結構

```
D:\Claude\德撲\
├── multiplayer\
│   ├── public\
│   │   └── index.html            ← 整個前端（HTML + CSS + JS 單檔）
│   ├── server.js                 ← Node + Express + ws WebSocket server
│   ├── game-engine.js            ← 純邏輯引擎（牌庫、評牌、AI、talent/power 規則）
│   ├── package.json
│   └── package-lock.json
├── 德撲錦標賽.html                  ← 早期單機版（可作為單機版起點參考）
├── 進度報告.md                     ← 早期進度紀錄
└── 遊戲設計與進度.md                 ← 本檔案
```

### 技術選擇

- **前端**：vanilla HTML / CSS / JS，無框架。CSS 全內嵌在 index.html 裡。
- **後端**：Node.js + Express + ws (WebSocket)
- **遊戲邏輯**：獨立的 `game-engine.js`，純函數風格，無 DOM 依賴 — **單機版可直接拿這個 module 用**
- **資料庫**：無（all in-memory，房間關掉資料就沒了）

### 通訊協定（WebSocket 訊息）

Client → Server：
- `create-room` / `join-room` / `leave-room`
- `select-talent` / `select-power`
- `update-settings`（房主改變體玩法、難度等）
- `start-game`
- `action`（fold / check / call / raise / allin）

Server → Client：
- `room-created` / `room-joined` / `lobby-update`
- `game-started` / `game-state` / `action-result`
- `street`（翻牌 / 轉牌 / 河牌）
- `showdown` / `triggers`（角色 / 能力觸發）
- `power-pick-pending` / `choose-power` / `power-applied`
- `stage-picks-start` / `stage-pick-submitted` / `stage-picks-end`
- `blind-up` / `blind-tick`
- `tournament-end`

---

## 三、核心遊戲循環

### 一場錦標賽的流程

```
大廳（lobby）
    ↓ 房主開房 / 玩家加入
等待室（wait）
    ↓ 選暱稱 / 頭貼 / 角色天賦 / 變體玩法 / AI 數量
    ↓ 房主按「開始遊戲」（前提：所有真人都選好角色）
遊戲開始
    ↓
┌─────────── 一手牌 ───────────┐
│  發底牌 → preflop → flop →   │
│  turn → river → showdown    │
│                              │
│  KO 別人 → 抽 N 個能力        │
│  （N = 同手 KO 人數）         │
└──────────────────────────────┘
    ↓
升盲檢查
    ↓ 升到 BB 400 / BB 1600 → 階段獎勵：所有人同時挑能力
    ↓
下一手...
    ↓
只剩一人 → 結算 → 回大廳
```

### 盲注節奏（依房主設定的 blindMs 推進）

| 等級 | SB / BB | 階段獎勵 |
|---|---|---|
| 1 | 100 / 200 | — |
| 2 | 200 / 400 | ✅ 全員各選 1 個能力 |
| 3 | 400 / 800 | — |
| 4 | 800 / 1600 | ✅ 全員各選 1 個能力 |
| 5+ | 1600 / 3200 起 | — |

升盲不會中斷一手牌，等本手結束再升。

---

## 四、角色天賦系統（12 個）

每位玩家在大廳挑一個角色（可重複，多人可選同一個），整場錦標賽都生效。可選「🎲 隨機」進遊戲後才揭曉。

| ID | 名稱 | 觸發 | 效果 |
|---|---|---|---|
| `soft_target` | 🍅 專挑軟柿子 | 比牌獲勝 | 從場上籌碼最少的對手額外多收 1 SB |
| `gold_kick` | 🦵 黃金右腳 | 比牌時輸在 kicker | 所有對手付你 3 SB 補償 |
| `rich_richer` | 👑 強者恆強 | 比牌獲勝且你是場上籌碼最多 | 每位輸家額外付他們本手投入底池的 5% |
| `rising_tide` | 🌊 水漲船高 | 你加注後對手跟注 | 跟注者每人額外付 1 SB **到底池**（不是給你） |
| `imposing` | 💢 氣勢凌人 | 你加注時 | 加注 target 少付 1 SB（all-in 不適用） |
| `bully` | 👹 惡霸 | preflop 你加注後全員棄牌 | 每位棄牌對手再付你 0.5 SB |
| `street_king` | 🥊 街頭小霸王 | 比牌獲勝 | 每位輸家額外付你 2 BB |
| `precision` | 🎯 精準出擊 | 你在 SB / BB 位棄牌 | 拿回 1 SB |
| `big_blind` | 🙈 大盲 | 整局 | 你看不到自己底牌（全黑）；比牌獲勝時每位輸家額外付他們本手投入底池的 100% |
| `small_blind` | 🙉 小盲 | 整局 | 你只看得到一張底牌；比牌獲勝時每位輸家額外付他們本手投入底池的 50% |
| `advance` | 💳 預支人生 | All-in 輸到 0 籌碼 | 自動向籌碼最多的對手借 1 BB 續命（一場限一次） |
| `east_money` | 💰 東家錢 | 每局開始 | 50% 機率向上一局贏家抽 1 SB |

---

## 五、能力系統（17 個）

由 KO 或階段獎勵獲得，3 選 1。同個能力同人不會抽到第二次。

| ID | 名稱 | 觸發 | 效果 |
|---|---|---|---|
| `arrogant` | 🌟 不可一世 | 用「高牌」贏比牌 | 每位輸家額外付 1 BB |
| `p_rule27` | ✌️ 27 法則 | 底牌剛好 2&7 並贏牌 | 全場每位對手付 2 BB |
| `three_kind` | 3️⃣ 三條 | 用「三條」贏比牌 | 每位輸家額外付 4 BB |
| `pair_back` | 👯 天生一對 | 比牌輸但底牌是對子 | 拿回 2 BB |
| `two_pair` | ✨ 兩對！ | 用「兩對」贏比牌 | 每位輸家額外付 3 BB |
| `one_pair` | 👫 對子 | 用「一對」贏比牌 | 每位輸家額外付 2 BB |
| `magnet` | 🧲 吸金術 | 贏牌且 chips < pot/2 | 額外獲得 1 BB |
| `side_pot` | 🦝 邊池小偷 | 有邊池且你贏 | 從邊池額外偷 20% |
| `streak` | 🔥 連勝加成 | 連續贏 3 場後 | 之後每勝每位對手付 1 SB |
| `second_rule` | 🥈 老二法則 | 1v1 且你籌碼少 | 立刻隨機獲得一個額外能力 |
| `adventurer` | 🎲 冒險者 | 立刻 | 隨機選一個能力，發動時才知道 |
| `whetstone` | ⚒️ 磨刀石 | 淘汰對手的回合 | 額外拿到一個能力 |
| `comeback` | ↩️ 逆轉王者 | All-in 河牌反超 | 每位參與對手付 2 BB |
| `biteback` | 🦈 反咬一口 | All-in 戰勝較大籌碼對手 | 額外獲得對手剩餘籌碼 10% |
| `grit` | 💔 忍痛割愛 | 棄牌時投入超過 30% 籌碼 | 下一局免費跟大盲 |
| `second_life` | 🩹 第二條命 | All-in 輸後 | 莊家借你 1 個當下大盲 |
| `last_straw` | 🌾 救命稻草 | 結算後你籌碼最少 | 下一局第一個大盲免費 |

---

## 六、變體玩法（8 種，房主決定）

| ID | 名稱 | 效果 |
|---|---|---|
| `classic` | 經典傳奇 | 標準德州撲克 |
| `less` | 以少為多 | 拿掉小數字（隨難度遞減牌庫） |
| `joker` | 抽鬼牌 | 隨機拿掉幾個數字 |
| `bb_adv` | 大盲優勢 | 某個數字加權 |
| `sunk` | 沉沒成本 | preflop 棄牌罰款進底池 |
| `fast_think` | 快思快想 | 思考時間縮短 |
| `fast_blind` | 快速晉升 | 升盲變快 |
| `rule27` | 27 規則 | 拿到 2&7 贏牌→全桌付錢 |

每種變體有 LV 1 / 2 / 3 三檔強度。

---

## 七、特色機制（已實作）

### Side pot（邊池）

完整實作。不對等 all-in 時會正確切分主池 / 邊池，每個 pot 各自找 eligible 中最佳牌的人贏。Over-bet 未跟注的部分會在 `advance()` 階段立刻退回 over-bettor，pot 顯示永遠等於實際對打金額。

**演算法**（`_computeSidePots` in game-engine.js）：依 totalBet 升序排列玩家、逐層切池子，每層 pot 金額 = layerSize × contributors。

### Auto-runout（all-in 自動發牌）

當一條街結束、能繼續下注的人 ≤ 1（其他都 all-in），不再開新街給單獨那位玩家亂加注，server 一張一張發 turn / river 然後 showdown。

### KO bonus picks

任何階段，本手 KO N 人 → killer 連抽 N 個能力。獨立於階段獎勵存在。

### Stage transition picks（階段獎勵）

升盲到 BB 400 / BB 1600 時，所有活著的玩家「同時」收到能力選單，各自挑選。全部送出（或 30s 超時）後依座位順序揭曉每人選了什麼。

### 預支人生（advance talent）

擁有此天賦的玩家 all-in 輸到 0 籌碼時，自動向籌碼最多的對手借 1 BB 續命。一場錦標賽限用一次（`p._advanceUsed` flag）。

### 中途淘汰結算 modal

玩家被淘汰時跳一個結算彈窗：
- 大字「你被淘汰」+ 第 N 名
- 完整排名表（含對手頭貼 / 名字 / 狀態）
- 「繼續觀戰」/「回大廳」兩個選項

被淘汰時抑制對手能力選擇 UI（`_iAmOut` flag），免得淘汰後還被洗版。

### 詐唬連續性（AI multi-street barrel）

AI 上一條街詐唬 → 這街多 +25% 機率繼續詐唬，形成 multi-street bluff。`_bluffStreak` flag 每手歸零、做出非詐唬行為時自動重置。

### 加注 over-bet 自動退回

例如 A all-in 3000、B 只能 call 1000 → A 多投的 2000 在 `_refundUncalled` 立刻退給 A，pot 從 4000 變回 2000，視覺上不會出現「灌水池」。

---

## 八、UI / UX 功能

### 大廳 / 等待室
- 60 個 emoji 頭貼挑選（人物 / 動物 / 食物 / 物品），localStorage 持久
- AI 自動分配機器人系列 emoji（🤖 👾 🦾 ⚙️ 📟）
- 角色天賦可重複選（不同真實玩家可選同一角色）
- 「🎲 隨機」角色：進遊戲後才揭曉
- 房主開始按鈕：所有真人未選角色就鎖住
- 摺疊式遊戲規則簡介
- 邀請碼 + 複製連結

### 遊戲畫面
- 6 個座位環繞橢圓桌，positional CSS（百分比定位）
- 思考時間光圈（線性，不會跳回滿格）
- 能力 chip 在座位邊緣，點擊看說明
- 飛籌碼動畫（pot → 贏家，依 side pot 分配正確金額）
- 比牌大字牌型名（showdown highlight）
- 牌型升級提示（玩家本身、auto-runout 期間抑制）
- Action panel：fold / check / call / raise / allin + Min / ½ Pot / Pot / Max
- Raise floating panel：vertical slider，沒輪到時淡到 20%

### 角色 / 能力觸發卡片
- 可點擊關閉的卡片，含「為什麼觸發」描述
- 自動關閉 12 秒
- 同時最多 4 張，超過自動把最舊的擠掉
- 區分四種 flow 樣式：
  - 玩家 → 玩家轉移
  - 系統 → 玩家補償
  - 玩家 → 🪙 底池（水漲船高 / 沉沒成本）
  - 玩家自己被扣

### 手機版 RWD
- `@media (max-width: 768px)` 全套
- 牌桌變直式橢圓
- 座位 / 卡牌縮小
- 動作列變底部 sticky 全寬
- 加注 slider 縮小放右下角
- Sidebar 變抽屜（左側浮動工具列：📜 牌型 🏆 籌碼排行 🚪 離開）
- 抽屜點任意處可關
- iOS dvh + safe-area-inset-bottom

### 結算頁
- 顯示完整排名表（活著 / 第 X 手出局）
- 自己列高亮金色
- 冠軍跳「🎉 你是冠軍」標題

---

## 九、AI 設計（aiDecide）

位於 `game-engine.js` 末段。決策流程：

1. **蒙地卡羅勝率估計**（150 samples）：隨機補完剩餘公牌 + 對手底牌，跑完發給 evaluate7，計算自己贏的比例
2. **pot odds**：need / (pot + need)
3. **aggression**：0.45 ~ 0.65 隨機
4. **stackBB**：剩餘籌碼 / BB

決策樹：
- 短碼 (< 12 BB)：push / fold（勝率 > 45% 直接 all-in）
- preflop：勝率 > 65% raise、> 40% call、否則 fold
- flop+：勝率 > 70% value bet、> 45% call/check-raise、< 45% bluff or fold
- 詐唬：12% 基礎 + 25% streak boost = ~37% 連續詐唬機率

**它不會做的事**：slowplay、3-bet 詐唬、誘騙、對手建模、bet sizing 詐術、記得對手歷史。對新手 OK，對熟手會被讀穿。

---

## 十、踩過的坑（重大 bug 修復紀錄）

### 1. All-in 沒做 side pot
**症狀**：對手 1000 all-in、我 5000 call 後，我可以繼續加注，比輸的話 5000 全部給對手。
**根因**：showdown 直接把整池給整體最佳牌的人，沒按貢獻分層。
**修法**：實作 `_computeSidePots()`，依 totalBet 切分主池 / 邊池，每池各自找 eligible 中最佳牌。

### 2. All-in 後一瞬間秀 5 張公牌
**症狀**：all-in 後 client 先閃一下完整公牌，才被 street event 倒回去一張一張秀。
**根因**：`_autoRunout` 一次發完所有牌、接著 `_broadcastState` 把完整 community 傳給 client。
**修法**：autoRunout 不發牌只回傳「等下要發哪幾條街」，由 server setTimeout 序列裡逐張呼叫 deal + broadcast。

### 3. Turn timer 中段感覺變慢
**症狀**：思考時間光圈兩端快、中間慢。
**根因**：`.turn-danger::before` 在剩 15% 時重新宣告 `animation-name`，導致 turnDrain 重啟。
**修法**：把 danger flash 移到獨立 `::after`，drain 環永遠只跑 turnDrain linear 動畫。

### 4. 預支人生天賦沒生效
**症狀**：擁有預支人生天賦的玩家 all-in 輸了還是直接淘汰。
**根因**：`startHand` 的 `chips <= 0 → alive = false` 沒檢查 talent。
**修法**：在淘汰判斷前先檢查 `hasTalent(p, 'advance') && !p._advanceUsed`，找 lender 借錢。

### 5. 水漲船高的 chip flow 顯示錯
**症狀**：trigger 卡片顯示「跟注者 → 加注者 +100」，看起來像直接給玩家。
**根因**：trigger 的 `toId` 設成 raiser.id，但錢實際上進底池。
**修法**：改 `toId: null`、加 `byId/byName`，client 渲染成「跟注者 → 🪙 底池 +100」。

### 6. 冠軍還要選能力
**症狀**：KO 最後一個對手後還被叫去選能力，選完才跳結算頁。
**修法**：showdown 後檢查 `continuing <= 1`（含預支人生續命的人）→ 直接 endTournament，跳過 power-pick。

### 7. 階段選擇太慢（sequential → parallel）
**症狀**：升盲時所有人輪流選，要等很久。
**修法**：重做成 `_stagePickState` 並行收集，所有人同時選、AI server 端先決定、全送出後依序揭曉。

### 8. 被淘汰後還看到對手選什麼能力
**症狀**：被打掛後對手選能力的 banner / overlay 還會跳。
**修法**：`_iAmOut` flag，showdown 收到 `r.eliminated.includes(myId)` 立刻設 true，後續所有 power-pick UI 都被擋。

---

## 十一、單機版（roguelike 風格）方向建議

### 為什麼要做單機版

從多人測試的反饋：**輕度 meta + 標準德撲對非德撲玩家不夠抓人**，因為：
- 「+1 SB」這種數值修正太溫和，對比 Balatro 動輒 100x、1000x、1,000,000 籌碼指數爆炸的爽感差太多
- 學習負擔太重（規則 + 角色 + 能力 + 變體一次塞）
- 缺少「角色成長 / build 累積」的長期記憶點

單機版能讓你**自由放大數值、加入更瘋的合招**，不用擔心多人平衡。

### 核心改造方向

#### A. 數值指數化

把所有「+N SB / +N BB」線性效果改成**乘法 / 滾雪球**：

```
舊：強者恆強 → 獲勝多收 5% pot
新：強者恆強 → 獲勝後下一手底池倍率 +20%（可疊加，三次連勝就 +60%）

舊：街頭小霸王 → 每位輸家付 2 BB
新：街頭小霸王 → 每位輸家付 (2 BB × 你的 KO 數)，KO 越多越貴

舊：連勝加成 → 連勝 3 場後每勝每對手 +1 SB
新：連勝加成 → 連勝 N 場 → 下手底池倍率 ×(1.2^N)
```

#### B. 加可見的進度條 / 滾雪球視覺

- 螢幕上常駐「Streak: 3」「Multiplier: ×4.5」這種 HUD
- 觸發時數字爆紅 / 螢幕震動 / 籌碼動畫加大
- 每手結束秀「你這手淨賺 +12,500（基礎 2000 × ×6.25 加成）」

#### C. 加「Run」結構（每場都不一樣）

像 Balatro / 殺戮尖塔：
- 玩家有一個 Run（完整一場錦標賽）
- 每升盲關卡選 1 個能力，從**重抽 / 升級 / 商店**三種路徑挑
- 失敗後重新開始，但解鎖新角色 / 新能力卡

#### D. Boss / 特殊 AI

不是 6 個普通 AI，而是 5 場關卡：
- 關卡 1 ~ 4：弱 AI、限制條件（例如「對手只玩 10% 範圍」）
- 關卡 5：Boss AI，有自己的特殊規則（例如「Boss 不會看你底牌但每手必加注」）

#### E. 商店系統

關卡之間有商店：
- 用本場累積 chip 買能力（rare / epic / legendary 等級）
- 重抽現有能力
- 買「修改器」（改變遊戲規則的東西）

#### F. 永久解鎖（meta-progression）

- 第一次玩只能用「強者恆強」「街頭小霸王」幾個基礎角色
- 通關 / 達成成就解鎖更多角色 + 起始能力
- 解鎖更難的關卡（Stake 1-8 之類）

### 技術可行性

**game-engine.js 已經是純邏輯模組**，可以幾乎直接 port 到單機版：
- 拿掉 server-side multiplayer 邏輯（房間、broadcast、power-pick 流程）
- 把 `aiDecide` 換成多種 AI personality
- 加 `gameState` 的存檔機制（localStorage 或 IndexedDB）
- UI 從 multiplayer 那套撥牌桌改成單機更精簡的 HUD

建議**新開一個專案資料夾**而不是 fork — 多人版繼續 maintain，單機版自由設計。

### 推薦的參考

- **Balatro**：撲克 + 狂滾數值的代表作，看它的 multiplier UI、jokers 設計
- **Slay the Spire**：roguelike 結構的標竿，看它的關卡圖、商店、寶箱
- **Luck Be A Landlord**：吃角子老虎 + roguelike，看它怎麼把「機率事件」變得有策略感
- **Inscryption**：另類撲克 roguelike，看劇情怎麼包裝牌局
- **Dicey Dungeons**：骰子 + roguelike，看它的能力排列組合系統

---

## 十二、多人版下一步可以做的事

如果還想繼續打磨多人版：

1. **持久玩家檔案**：localStorage UUID + Neon / Supabase Postgres，記錄每個玩家的勝率 / KO 數 / 常拿到的能力
2. **leaderboard**：每週 / 全時排行榜
3. **再來一局**：結算頁加一鍵直接重開房間、保留玩家
4. **變體玩法 LV3 內容**：目前 LV3 多數是空白，可填入更刺激的規則
5. **觀戰模式優化**：被淘汰玩家「繼續觀戰」目前只是看畫面，可以加聊天 / 預測誰會贏
6. **行動裝置橫向 layout**：目前只調直向

---

## 十三、聯絡 / 部署

- **Render 部署**：https://poker-tournament-4n94.onrender.com/
- **Repo**：本機 D:\Claude\德撲（git 在 .git）
- **Render 限制**：
  - 免費 web service：750 hr/month、15 分鐘無流量會 sleep
  - 沒有持久磁碟（filesystem 重啟會清空）
  - 要存資料必須接外部 DB（Neon / Supabase）

---

## 十四、做得好的地方 / 做不好的地方（自評）

### 做得好
- Side pot 邏輯數學上完整正確（測過 8 種 case）
- AI 蒙地卡羅勝率估計合理，至少不會做白癡決策
- RWD 在手機上實際可玩
- 主流程（lobby → wait → game → showdown → end）動線順暢
- 觸發卡片有 desc，玩家能理解「為什麼這個能力觸發了」

### 做不好 / 可改進
- 數值太溫和，對非德撲玩家沒記憶點（→ 單機版方向）
- 學習負擔重（一次塞 12 角色 + 17 能力 + 8 變體）
- 牌桌動畫缺乏「狂歡感」（贏大 pot 沒有大爆炸特效）
- 沒有持久玩家檔案 / 進度
- 沒有教學 / 新手引導
- AI 偏單純，老手會被讀穿

---

**最後更新**：2026-05-04
**作者**：Chuan + Claude
